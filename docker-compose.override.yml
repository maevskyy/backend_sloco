services:
  backend:
    build:
      context: ./services/gateway
      dockerfile: Dockerfile
    image: sloco-backend:local

  recommendation-service:
    build:
      context: ./services/recommendation
      dockerfile: Dockerfile
    image: sloco-recommendation-service:local
    ports:
      - "127.0.0.1:8000:8000"
